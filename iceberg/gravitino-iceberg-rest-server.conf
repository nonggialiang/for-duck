# Catalog 后端（默认 memory，开发用最简单）
  gravitino.iceberg-rest.catalog-backend = memory
  gravitino.iceberg-rest.warehouse = /tmp/iceberg-warehouse

  # 多 catalog 示例（可选）
  # gravitino.iceberg-rest.catalog.my_catalog.catalog-backend = memory
  # gravitino.iceberg-rest.catalog.my_catalog.warehouse = /tmp/my_catalog

  # Spring Boot 端口
  server.port = 9001